const totalFoodTrip = localStorage.getItem("totalFoodTrip");

// Use the total value as needed (e.g., logging to the console)
if (totalFoodTrip !== null) {
    console.log("Total Food Trip Cost:", totalFoodTrip);
} else {
    console.log("No total food trip cost found.");
}

const totaltravelTrip = localStorage.getItem("totalFoodTrip");

// Use the total value as needed (e.g., logging to the console)
if (totalFoodTrip !== null) {
    console.log("Total Food Trip Cost:", totalFoodTrip);
} else {
    console.log("No total food trip cost found.");
}
const totalacoomodationTrip = localStorage.getItem("totalFoodTrip");

// Use the total value as needed (e.g., logging to the console)
if (totalFoodTrip !== null) {
    console.log("Total Food Trip Cost:", totalFoodTrip);
} else {
    console.log("No total food trip cost found.");
}

const other = 0;

const entertainement = 0